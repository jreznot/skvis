package ru.ssau.templet

/**
 * @author Yuriy Artamonov 
 */
package object liza {

  type TimeStamp = Long
  val TimeStamp = (x: Long) => x

  type TimeInterval = Long
  val TimeInterval = (x: Long) => x

  type Count = Int
}
